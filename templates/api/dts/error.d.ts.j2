{{ error.doc_comment }}export declare class {{ error.name }} extends globalThis.Error {
  readonly tag: string;
  protected constructor(tag: string, message?: string);
}{% for variant in error.variants %}

{{ variant.doc_comment }}export declare class {{ variant.class_name }} extends {{ error.name }} {
  readonly tag: {{ variant.tag_literal }};
{% for field in variant.fields %}{{ field.doc_comment }}  readonly {{ field.property_name }}: {{ field.type_name }};
{% endfor %}  constructor({{ variant.constructor_params }});
}{% endfor %}
