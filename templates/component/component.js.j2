import { ffiFunctions } from "./{{ namespace }}-ffi.js";
import { createForeignBytes, RustBufferValue } from "./runtime/ffi-types.js";
import {
  AbstractFfiConverterByteArray,
  FfiConverterArray,
  FfiConverterBool,
  FfiConverterBytes,
  FfiConverterDuration,
  FfiConverterFloat32,
  FfiConverterFloat64,
  FfiConverterInt8,
  FfiConverterInt16,
  FfiConverterInt32,
  FfiConverterInt64,
  FfiConverterMap,
  FfiConverterOptional,
  FfiConverterString,
  FfiConverterTimestamp,
  FfiConverterUInt8,
  FfiConverterUInt16,
  FfiConverterUInt32,
  FfiConverterUInt64,
} from "./runtime/ffi-converters.js";
import { UnexpectedEnumCase } from "./runtime/errors.js";
import { rustCallAsync } from "./runtime/async-rust-call.js";
import {
  createObjectConverter,
  createObjectFactory,
  UniffiObjectBase,
} from "./runtime/objects.js";
import { defaultRustCaller } from "./runtime/rust-call.js";

export const componentMetadata = Object.freeze({
  namespace: {{ namespace_json }},
  packageName: {{ package_name_json }},
  cdylibName: {{ cdylib_name_json }},
  nodeEngine: {{ node_engine_json }},
  libPathLiteral: {{ lib_path_literal_json }},
  manualLoad: {{ manual_load }},
});

export { ffiMetadata } from "./{{ namespace }}-ffi.js";
{% if manual_load %}
export { load, unload } from "./{{ namespace }}-ffi.js";
{% endif %}

{{ public_api_js }}
